spring:
  application:
    name: request-distribution-service
  kafka:
    bootstrap-servers: localhost:9092
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
server:
  port: 0

